apply plugin: "war"

bootWar {
    mainClassName = "com.sysco.dpx.bff.DpxApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
